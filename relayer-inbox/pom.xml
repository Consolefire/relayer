<?xml version="1.0" encoding="UTF-8"?>
<project xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xmlns="http://maven.apache.org/POM/4.0.0"
    xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <artifactId>relayer-inbox</artifactId>
    <dependencies>
        <dependency>
            <artifactId>relayer-model</artifactId>
            <groupId>${project.groupId}</groupId>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <dependency>
                <artifactId>relayer-model</artifactId>
                <groupId>${project.groupId}</groupId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <artifactId>relayer-utils</artifactId>
                <groupId>${project.groupId}</groupId>
                <version>${project.version}</version>
            </dependency>
            <dependency>
                <artifactId>relayer-core</artifactId>
                <groupId>${project.groupId}</groupId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>
    <modelVersion>4.0.0</modelVersion>
    <modules>
        <module>relayer-inbox-model</module>
        <module>relayer-inbox-core</module>
        <module>relayer-inbox-writer</module>
        <module>relayer-inbox-app</module>
    </modules>

    <packaging>pom</packaging>


    <parent>
        <artifactId>relayer</artifactId>
        <groupId>com.consolefire.relayer</groupId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

</project>